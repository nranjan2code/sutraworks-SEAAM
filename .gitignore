# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env

# MacOS
.DS_Store

# SEAAM Runtime Generation (Soma)
# Evolved code is kept in the soma/ directory and ignored from version control.
soma/
events.log
memory_journal.json

# Keep empty init files if needed, but the system creates folders.
# We explicitly want to preserve the Kernel and Cortex.
# seaam/kernel/ is tracked.
# seaam/cortex/ is tracked.
dna.json
