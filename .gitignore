# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env

# MacOS
.DS_Store

# SEAA Runtime Generation (Soma)
# Core organs that ship with the system are tracked.
# Runtime-evolved organs are ignored to prevent noise.
#
# Tracking core organs:
#   soma/perception/
#   soma/memory/
#   soma/interface/
#   soma/storage/
#   soma/extensions/
#   soma/learning/
#
# Ignoring runtime generated code:
soma/generated/
soma/experimental/

# Log files (generated at runtime)
events.log
memory_journal.json

# Keep empty init files if needed, but the system creates folders.
# We explicitly want to preserve the Kernel and Cortex.
# seaa/kernel/ is tracked.
# seaa/cortex/ is tracked.
dna.json
.identity.json
.dna_backups/
.pytest_cache/
*.bak
